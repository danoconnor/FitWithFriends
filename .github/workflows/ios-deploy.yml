name: App Store Upload

on:
  push:
    branches: [ "main" ]
    paths:
      - 'Clients/iOS/**'
  workflow_dispatch:

env:
  XCODE_PROJECT_DIR: ./Clients/iOS/FitWithFriends
  WORKSPACE_NAME: FitWithFriends.xcworkspace
  SCHEME: FitWithFriends

jobs:
  deploy:
    name: Build and upload to App Store Connect
    runs-on: macos-26

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Set up App Store Connect API key
        env:
          APP_STORE_CONNECT_API_KEY_CONTENT_BASE64: ${{ secrets.APP_STORE_CONNECT_API_KEY_CONTENT_BASE64 }}
          AUTH_KEY_ID: ${{ secrets.APP_STORE_CONNECT_API_KEY_ID }}
        run: |
          mkdir -p ~/.private_keys
          echo "$APP_STORE_CONNECT_API_KEY_CONTENT_BASE64" | base64 --decode \
            > ~/.private_keys/AuthKey_${AUTH_KEY_ID}.p8

      - name: Install certificates
        env:
          DISTRIBUTION_CERTIFICATE_P12_BASE64: ${{ secrets.DISTRIBUTION_CERTIFICATE_P12_BASE64 }}
          DISTRIBUTION_CERTIFICATE_PASSWORD: ${{ secrets.DISTRIBUTION_CERTIFICATE_PASSWORD }}
          DEVELOPMENT_CERTIFICATE_P12_BASE64: ${{ secrets.DEVELOPMENT_CERTIFICATE_P12_BASE64 }}
          DEVELOPMENT_CERTIFICATE_PASSWORD: ${{ secrets.DEVELOPMENT_CERTIFICATE_PASSWORD }}
          KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
        run: |
          security create-keychain -p "$KEYCHAIN_PASSWORD" build.keychain
          security list-keychains -d user -s build.keychain login.keychain
          security default-keychain -s build.keychain
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" build.keychain
          security set-keychain-settings -t 3600 -u build.keychain

          echo "$DISTRIBUTION_CERTIFICATE_P12_BASE64" | base64 --decode > /tmp/dist_cert.p12
          security import /tmp/dist_cert.p12 -k build.keychain \
            -P "$DISTRIBUTION_CERTIFICATE_PASSWORD" \
            -T /usr/bin/codesign \
            -T /usr/bin/xcodebuild
          rm /tmp/dist_cert.p12

          # Install a fixed development certificate so -allowProvisioningUpdates
          # does not create a new one on each run (Apple caps active dev certs).
          echo "$DEVELOPMENT_CERTIFICATE_P12_BASE64" | base64 --decode > /tmp/dev_cert.p12
          security import /tmp/dev_cert.p12 -k build.keychain \
            -P "$DEVELOPMENT_CERTIFICATE_PASSWORD" \
            -T /usr/bin/codesign \
            -T /usr/bin/xcodebuild
          rm /tmp/dev_cert.p12

          security set-key-partition-list -S apple-tool:,apple: -s \
            -k "$KEYCHAIN_PASSWORD" build.keychain

      - name: Build archive
        working-directory: ${{ env.XCODE_PROJECT_DIR }}
        env:
          AUTH_KEY_ID: ${{ secrets.APP_STORE_CONNECT_API_KEY_ID }}
          AUTH_KEY_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_API_KEY_ISSUER_ID }}
          TEAM_ID: ${{ secrets.TEAM_ID }}
          FWF_KEYCHAIN_PREFIX: ${{ secrets.FWF_KEYCHAIN_PREFIX }}
          FWF_SUPPORT_EMAIL: ${{ secrets.FWF_SUPPORT_EMAIL }}
          FWF_APP_PROTOCOL: ${{ secrets.FWF_APP_PROTOCOL }}
          FWF_DEVELOPER_NAME: ${{ secrets.FWF_DEVELOPER_NAME }}
          FWF_PROD_SERVER_URL: ${{ secrets.FWF_PROD_SERVER_URL }}
          FWF_PROD_CLIENT_SECRET: ${{ secrets.FWF_PROD_CLIENT_SECRET }}
          FWF_PROD_CLIENT_ID: ${{ secrets.FWF_PROD_CLIENT_ID }}
        run: |
          xcodebuild archive \
            -workspace "$WORKSPACE_NAME" \
            -scheme "$SCHEME" \
            -configuration Release \
            -destination "generic/platform=iOS" \
            -archivePath "$RUNNER_TEMP/FitWithFriends.xcarchive" \
            -authenticationKeyPath ~/.private_keys/AuthKey_${AUTH_KEY_ID}.p8 \
            -authenticationKeyID "$AUTH_KEY_ID" \
            -authenticationKeyIssuerID "$AUTH_KEY_ISSUER_ID" \
            -allowProvisioningUpdates \
            DEVELOPMENT_TEAM="$TEAM_ID"

      - name: Export and upload to App Store Connect
        env:
          AUTH_KEY_ID: ${{ secrets.APP_STORE_CONNECT_API_KEY_ID }}
          AUTH_KEY_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_API_KEY_ISSUER_ID }}
        run: |
          xcodebuild -exportArchive \
            -archivePath "$RUNNER_TEMP/FitWithFriends.xcarchive" \
            -exportOptionsPlist "${{ env.XCODE_PROJECT_DIR }}/ExportOptions.plist" \
            -exportPath "$RUNNER_TEMP/export" \
            -authenticationKeyPath ~/.private_keys/AuthKey_${AUTH_KEY_ID}.p8 \
            -authenticationKeyID "$AUTH_KEY_ID" \
            -authenticationKeyIssuerID "$AUTH_KEY_ISSUER_ID" \
            -allowProvisioningUpdates

      - name: Clean up
        if: always()
        env:
          AUTH_KEY_ID: ${{ secrets.APP_STORE_CONNECT_API_KEY_ID }}
        run: |
          security delete-keychain build.keychain || true
          rm -f ~/.private_keys/AuthKey_${AUTH_KEY_ID}.p8 || true
